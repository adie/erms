%% -*- erlang -*-
%% ex: ft=erlang et
{application, erms,
 [{description, "erms"},
  {vsn, "0.0.1"},
  {modules, [erms, erms_app, erms_deps, erms_sup, erms_server, gen_server_cluster]},
  {registered, []},
  {mod, {erms_app, []}},
  {applications, [kernel, stdlib, sasl]},
  {env, []}
]}.
